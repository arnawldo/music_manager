<h1>New Spotify credential</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.spotify_credential_path(@conn, :create)) %>

<span><%= link "Back", to: Routes.spotify_credential_path(@conn, :index) %></span>
