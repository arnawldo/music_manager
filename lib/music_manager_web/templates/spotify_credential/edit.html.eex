<h1>Edit Spotify credential</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.spotify_credential_path(@conn, :update, @spotify_credential)) %>

<span><%= link "Back", to: Routes.spotify_credential_path(@conn, :index) %></span>
